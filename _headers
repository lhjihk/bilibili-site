# Cloudflare Pages 响应头配置
# 毛笔字体（1.3~2.6MB）永久缓存：文件名都带 ?v= 版本号，改版重裁时会换号（见维护手册重裁流程），
# 所以 immutable 安全，浏览器一次下完之后就不再回源校验/重下，手机(微信/Safari)重复打开秒出。
# 只作用于字体目录，不影响 content.json / html / css / js / 图片 的既有缓存策略。
/assets/fonts/*
  Cache-Control: public, max-age=31536000, immutable
